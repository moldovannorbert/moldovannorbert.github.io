{
  "name": "moldovannorbert.github.io",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/export-corpus.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "npm run prebuild && astro build",
    "collab:fetch": "node scripts/fetch-collab-graph.mjs"
  },
  "dependencies": {
    "astro": "^7.1.3",
    "gray-matter": "^4.0.3",
    "yaml": "^2.8.0",
    "zod": "^4.0.5"
  }
}
